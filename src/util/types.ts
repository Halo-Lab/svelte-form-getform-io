export type FormSubmitData = Record<string, any>

export type RadioGroupOption = {
    id: string;
    label: string;
    disabled?: boolean;
}

export type SelectOption = {
    id: string;
    label: string;
    disabled?: boolean;
}